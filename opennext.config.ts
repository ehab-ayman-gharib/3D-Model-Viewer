import type { OpenNextConfig } from "@opennextjs/aws/types/open-next.js";

// @ts-ignore - Bypass type checking for older OpenNext versions
const config: any = {
  default: {},
  imageOptimization: false,
};

export default config;
